/**
 * Backend AppRouter Type
 * 
 * This file provides the AppRouter type for frontend use.
 * 
 * Note: The actual AppRouter type should be imported from the backend
 * after it's built. For now, we use 'any' as a placeholder.
 */

// Placeholder type - will be replaced with actual AppRouter from backend
export type AppRouter = any;

// TODO: After backend is built, replace with:
// export type { AppRouter } from '../../../backend/src/trpc/routers/_app';
